% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lib_timeline.R
\name{lib_timeline}
\alias{lib_timeline}
\title{Plot a timeline of when your packages were installed}
\usage{
lib_timeline(since = NULL, by_library = TRUE)
}
\arguments{
\item{since}{Optional \code{Date} (or a string coercible to one, e.g.
\code{"2025-01-01"}). If supplied, only packages installed on or after this
date are included.}

\item{by_library}{Color points by library path? Defaults to \code{TRUE}.}
}
\value{
Invisibly, a \code{data.frame} with columns \code{Package}, \code{LibPath}, and
\code{InstallDate}, for the packages included in the plot.
}
\description{
Builds a simple timeline showing when each installed package was last
(re)installed, using the modification time of each package's
\code{DESCRIPTION} file as a proxy for its install date. This makes it easy to
spot bursts of installation activity (e.g. setting up for a course or
project) versus quiet stretches.
}
\examples{
lib_timeline()
lib_timeline(since = "2025-01-01")
}
